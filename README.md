# CarPricePredictor
AutoValue is an AI-powered web app that predicts car prices based on specs like fuel type, car body, and engine size. Built with Flask, HTML, and a Random Forest model trained on a Kaggle dataset, it offers real-time predictions through a simple, responsive UI.
